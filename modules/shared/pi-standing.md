# Pi standing instructions

- Pi is the **cheap** hub. Default chat: OpenCode Zen `nemotron-3-ultra-free`.
  Overflow also: `--kind hermes` (Nous $20 OSS, not GPT-5/Claude) and `cmd`
  (GOAT Open Source). Check `hermes insights --days 7` / `cmd status`.
  Paid Muse / Cursor = `--kind muse` / `--kind cursor` on a **worktree**.
  Never `--kind grok`. Never muse/cursor on a primary checkout.
- New chat per packet. Rewind with `/tree`. Plugin auto-starts Pi; coordinator
  `/quit`s and starts the native kind for a paid packet.
- Visuals: `/model minicpm-v-4.5`. Never `*-contributor*` /
  `cursor/muse-spark-1.3@*`. Search: one-grep with absolute `root`.
- Large dumps: Headroom before re-entry. obs-pack `~/.pi/agent/obs/` (`--kind pi`
  only). `/compact` at a finished subtask (`:8091`). Native Cursor/Muse: last
  hunk+Gate in the same shell; one-grep `root` = this worktree.
- Durable facts go in git or `memory_*`. Work start: `overflow-assign`.
  Reviewers: `--kind muse` or `--kind cursor` (Headroom + worktree `root` +
  one shell after edit). Ladder: `~/.pi/agent/stack.md`.
  Spawn skill: Nix copy under `~/.pi/agent/skills/herdr-pi-model-spawn/` only.
